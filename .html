<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Ranova Memecoin</title>
  <style>
    :root {
      --bg: #0f1724;
      --card: #071226;
      --accent: #7c3aed;
      --muted: #9aa4b2;
      color-scheme: dark;
    }

    * { box-sizing: border-box; }
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(180deg, #001219, #041024);
      color: #e6eef6;
      margin: 0;
      padding: 0;
    }

    header {
      background: rgba(0,0,0,0.3);
      padding: 1rem;
      text-align: center;
    }

    header h1 {
      margin: 0;
      font-size: 1.8rem;
    }

    nav a {
      margin: 0 10px;
      color: var(--muted);
      text-decoration: none;
      font-weight: bold;
    }

    nav a:hover {
      color: white;
    }

    main {
      max-width: 500px;
      margin: 2rem auto;
      background: rgba(255,255,255,0.05);
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0,0,0,0.3);
    }

    h2 {
      text-align: center;
    }

    .section {
      display: none;
    }

    .active {
      display: block;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    label {
      font-size: 0.9rem;
    }

    input {
      padding: 10px;
      border-radius: 8px;
      border: 1px solid rgba(255,255,255,0.2);
      background: rgba(255,255,255,0.05);
      color: white;
    }

    button {
      padding: 10px;
      border: none;
      border-radius: 8px;
      background: var(--accent);
      color: white;
      font-weight: bold;
      cursor: pointer;
    }

    .msg {
      color: #f88;
      font-size: 0.9rem;
    }

    .dashboard {
      text-align: center;
    }

    footer {
      text-align: center;
      margin: 2rem 0;
      font-size: 0.8rem;
      color: var(--muted);
    }
  </style>
</head>
<body>

<header>
  <h1>🚀 Ranova Memecoin</h1>
  <nav>
    <a href="#" onclick="showSection('coming')">Coming Soon</a>
    <a href="#" onclick="showSection('signup')">Sign Up</a>
    <a href="#" onclick="showSection('login')">Login</a>
    <a href="#" onclick="showDashboard()">Dashboard</a>
    <a href="#" onclick="logout()">Logout</a>
  </nav>
</header>

<main>
  <!-- Coming Soon -->
  <section id="coming" class="section active">
    <h2>🚧 Coming Soon</h2>
    <p>Exciting things are on the way at Ranova!</p>
    <p>Stay tuned for new features, memecoin mechanics, and more. 🌕</p>
  </section>

  <!-- Signup -->
  <section id="signup" class="section">
    <h2>Sign Up</h2>
    <form id="signupForm">
      <label>
        Username
        <input type="text" id="signupUsername" required minlength="3">
      </label>
      <label>
        Password
        <input type="password" id="signupPassword" required minlength="6">
      </label>
      <button type="submit">Create Account</button>
      <div class="msg" id="signupMsg"></div>
    </form>
  </section>

  <!-- Login -->
  <section id="login" class="section">
    <h2>Login</h2>
    <form id="loginForm">
      <label>
        Username
        <input type="text" id="loginUsername" required>
      </label>
      <label>
        Password
        <input type="password" id="loginPassword" required>
      </label>
      <button type="submit">Log In</button>
      <div class="msg" id="loginMsg"></div>
    </form>
  </section>

  <!-- Dashboard -->
  <section id="dashboard" class="section">
    <h2>Dashboard</h2>
    <div class="dashboard">
      <p>Welcome, <strong id="dashUser"></strong>!</p>
      <p>Your Ranova Memecoin balance:</p>
      <h3>💰 <span id="dashBalance">0</span> RANO</h3>
      <button onclick="mint()">Mint +10 RANO</button>
    </div>
  </section>
</main>

<footer>
  &copy; 2025 Ranova Memecoin. Not real crypto. For fun only.
</footer>

<script>
  let currentUser = null;

  // Empty users by default (no demo)
  if (!localStorage.getItem('users')) {
    localStorage.setItem('users', JSON.stringify({}));
  }

  function showSection(id) {
    document.querySelectorAll(".section").forEach(s => s.classList.remove("active"));
    document.getElementById(id).classList.add("active");
  }

  function getUsers() {
    return JSON.parse(localStorage.getItem("users") || "{}");
  }

  function saveUsers(users) {
    localStorage.setItem("users", JSON.stringify(users));
  }

  // Signup
  document.getElementById("signupForm").onsubmit = function(e) {
    e.preventDefault();
    const username = document.getElementById("signupUsername").value.trim();
    const password = document.getElementById("signupPassword").value;

    const users = getUsers();
    if (users[username]) {
      document.getElementById("signupMsg").textContent = "Username already exists.";
      return;
    }

    users[username] = { password, balance: 1000 };
    saveUsers(users);
    currentUser = username;
    updateDashboard();
    showSection("dashboard");
  };

  // Login
  document.getElementById("loginForm").onsubmit = function(e) {
    e.preventDefault();
    const username = document.getElementById("loginUsername").value.trim();
    const password = document.getElementById("loginPassword").value;

    const users = getUsers();
    if (!users[username] || users[username].password !== password) {
      document.getElementById("loginMsg").textContent = "Invalid username or password.";
      return;
    }

    currentUser = username;
    updateDashboard();
    showSection("dashboard");
  };

  function updateDashboard() {
    const users = getUsers();
    const user = users[currentUser];
    document.getElementById("dashUser").textContent = currentUser;
    document.getElementById("dashBalance").textContent = user.balance;
  }

  function showDashboard() {
    if (!currentUser) {
      alert("Please log in first.");
      return;
    }
    updateDashboard();
    showSection("dashboard");
  }

  function mint() {
    const users = getUsers();
    if (!currentUser) return;
    users[currentUser].balance += 10;
    saveUsers(users);
    updateDashboard();
  }

  function logout() {
    currentUser = null;
    alert("Logged out!");
    showSection("coming");
  }
</script>

</body>
</html>
